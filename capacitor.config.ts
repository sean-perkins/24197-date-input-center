import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'date-input-center',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
